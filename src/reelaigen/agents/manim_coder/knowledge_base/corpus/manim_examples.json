{
  "source": {
    "name": "Manim example dataset",
    "origin": "User-provided examples collected from Manim",
    "notes": "Structured semantic retrieval dataset. Code is preserved exactly from the provided examples. Metadata fields are normalized for retrieval."
  },
  "examples": [
    {
      "id": "manim_ce_logo",
      "title": "ManimCELogo",
      "summary": "Creates the Manim Community logo from text and basic shapes.",
      "explanation": "Creates the Manim Community logo using mathematical text, circles, squares, and triangles with specific positioning and colors.",
      "keywords": ["logo", "shapes", "math text", "vgroup", "composition"],
      "example_text": "Build a static logo scene with MathTex and simple geometric shapes grouped together.",
      "scene_family": "shape_composition",
      "symbols": ["Scene", "MathTex", "Circle", "Square", "Triangle", "VGroup"],
      "tags": ["2d", "text", "math", "geometry", "group", "logo"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass ManimCELogo(Scene):\n    def construct(self):\n        self.camera.background_color = \"#ece6e2\"\n        logo_green = \"#87c2a5\"\n        logo_blue = \"#525893\"\n        logo_red = \"#e07a5f\"\n        logo_black = \"#343434\"\n        ds_m = MathTex(r\"\\mathbb{M}\", fill_color=logo_black).scale(7)\n        ds_m.shift(2.25 * LEFT + 1.5 * UP)\n        circle = Circle(color=logo_green, fill_opacity=1).shift(LEFT)\n        square = Square(color=logo_blue, fill_opacity=1).shift(UP)\n        triangle = Triangle(color=logo_red, fill_opacity=1).shift(RIGHT)\n        logo = VGroup(triangle, square, circle, ds_m)\n        logo.move_to(ORIGIN)\n        self.add(logo)"
    },
    {
      "id": "brace_annotation",
      "title": "BraceAnnotation",
      "summary": "Annotates a line with braces and labels.",
      "explanation": "Demonstrates how to use braces to annotate distances between dots, showing both horizontal and custom-angled labels.",
      "keywords": ["brace", "annotation", "distance", "line", "labels"],
      "example_text": "Show how Brace can label a line segment with plain text and TeX labels.",
      "scene_family": "annotation_scene",
      "symbols": ["Scene", "Dot", "Line", "Brace"],
      "tags": ["2d", "annotation", "brace", "line", "text", "math"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass BraceAnnotation(Scene):\n    def construct(self):\n        dot = Dot([-2, -1, 0])\n        dot2 = Dot([2, 1, 0])\n        line = Line(dot.get_center(), dot2.get_center()).set_color(ORANGE)\n        b1 = Brace(line)\n        b1text = b1.get_text(\"Horizontal distance\")\n        b2 = Brace(line, direction=line.copy().rotate(PI / 2).get_unit_vector())\n        b2text = b2.get_tex(\"x-x_1\")\n        self.add(line, dot, dot2, b1, b2, b1text, b2text)"
    },
    {
      "id": "vector_arrow",
      "title": "VectorArrow",
      "summary": "Places an arrow on a number plane with coordinate labels.",
      "explanation": "Visualizes a vector arrow starting from the origin to a coordinate (2,2) on a coordinate plane with text labels.",
      "keywords": ["arrow", "vector", "number plane", "coordinates", "labels"],
      "example_text": "Use Arrow and NumberPlane together with text labels at the origin and vector tip.",
      "scene_family": "vector_intro",
      "symbols": ["Scene", "Dot", "Arrow", "NumberPlane", "Text"],
      "tags": ["2d", "vector", "arrow", "graphing", "plane", "text"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass VectorArrow(Scene):\n    def construct(self):\n        dot = Dot(ORIGIN)\n        arrow = Arrow(ORIGIN, [2, 2, 0], buff=0)\n        numberplane = NumberPlane()\n        origin_text = Text('(0, 0)').next_to(dot, DOWN)\n        tip_text = Text('(2, 2)').next_to(arrow.get_end(), RIGHT)\n        self.add(numberplane, dot, arrow, origin_text, tip_text)"
    },
    {
      "id": "gradient_image_from_array",
      "title": "GradientImageFromArray",
      "summary": "Creates and displays an image from a NumPy array.",
      "explanation": "Generates a gradient image from a NumPy array and displays it with a surrounding green border.",
      "keywords": ["image", "numpy", "gradient", "border", "array"],
      "example_text": "Use ImageMobject with a NumPy array and surround it with a rectangle.",
      "scene_family": "image_display",
      "symbols": ["Scene", "ImageMobject", "SurroundingRectangle"],
      "tags": ["2d", "image", "bitmap", "numpy", "highlight"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass GradientImageFromArray(Scene):\n    def construct(self):\n        n = 256\n        imageArray = np.uint8([[i * 256 / n for i in range(0, n)] for _ in range(0, n)])\n        image = ImageMobject(imageArray).scale(2)\n        image.background_rectangle = SurroundingRectangle(image, color=GREEN)\n        self.add(image, image.background_rectangle)"
    },
    {
      "id": "boolean_operations",
      "title": "BooleanOperations",
      "summary": "Demonstrates boolean set operations between shapes.",
      "explanation": "Animates CSG-like boolean operations (Union, Intersection, Exclusion, Difference) between two overlapping ellipses.",
      "keywords": ["boolean operations", "union", "intersection", "difference", "shapes"],
      "example_text": "Show overlapping shapes and animate their boolean combinations with labels.",
      "scene_family": "boolean_shapes",
      "symbols": ["Scene", "Ellipse", "Intersection", "Union", "Exclusion", "Difference", "Group", "FadeIn", "Text", "MarkupText"],
      "tags": ["2d", "geometry", "shape", "animation", "transform", "text"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass BooleanOperations(Scene):\n    def construct(self):\n        ellipse1 = Ellipse(width=4.0, height=5.0, fill_opacity=0.5, color=BLUE, stroke_width=10).move_to(LEFT)\n        ellipse2 = ellipse1.copy().set_color(color=RED).move_to(RIGHT)\n        bool_ops_text = MarkupText(\"<u>Boolean Operation</u>\").next_to(ellipse1, UP * 3)\n        ellipse_group = Group(bool_ops_text, ellipse1, ellipse2).move_to(LEFT * 3)\n        self.play(FadeIn(ellipse_group))\n        i = Intersection(ellipse1, ellipse2, color=GREEN, fill_opacity=0.5)\n        self.play(i.animate.scale(0.25).move_to(RIGHT * 5 + UP * 2.5))\n        intersection_text = Text(\"Intersection\", font_size=23).next_to(i, UP)\n        self.play(FadeIn(intersection_text))\n        u = Union(ellipse1, ellipse2, color=ORANGE, fill_opacity=0.5)\n        union_text = Text(\"Union\", font_size=23)\n        self.play(u.animate.scale(0.3).next_to(i, DOWN, buff=union_text.height * 3))\n        union_text.next_to(u, UP)\n        self.play(FadeIn(union_text))\n        e = Exclusion(ellipse1, ellipse2, color=YELLOW, fill_opacity=0.5)\n        exclusion_text = Text(\"Exclusion\", font_size=23)\n        self.play(e.animate.scale(0.3).next_to(u, DOWN, buff=exclusion_text.height * 3.5))\n        exclusion_text.next_to(e, UP)\n        self.play(FadeIn(exclusion_text))\n        d = Difference(ellipse1, ellipse2, color=PINK, fill_opacity=0.5)\n        difference_text = Text(\"Difference\", font_size=23)\n        self.play(d.animate.scale(0.3).next_to(u, LEFT, buff=difference_text.height * 3.5))\n        difference_text.next_to(d, UP)\n        self.play(FadeIn(difference_text))"
    },
    {
      "id": "point_moving_on_shapes",
      "title": "PointMovingOnShapes",
      "summary": "Shows several basic animation primitives on a dot and circle.",
      "explanation": "Shows basic animation types including growing a circle, transforming a dot, moving it along a path, and rotating it.",
      "keywords": ["grow", "transform", "move along path", "rotate", "dot"],
      "example_text": "Use GrowFromCenter, Transform, MoveAlongPath, and Rotating in one simple scene.",
      "scene_family": "basic_animation_demo",
      "symbols": ["Scene", "Circle", "Dot", "Line", "GrowFromCenter", "Transform", "MoveAlongPath", "Rotating"],
      "tags": ["2d", "animation", "circle", "point", "transform", "movement"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass PointMovingOnShapes(Scene):\n    def construct(self):\n        circle = Circle(radius=1, color=BLUE)\n        dot = Dot()\n        dot2 = dot.copy().shift(RIGHT)\n        self.add(dot)\n        line = Line([3, 0, 0], [5, 0, 0])\n        self.add(line)\n        self.play(GrowFromCenter(circle))\n        self.play(Transform(dot, dot2))\n        self.play(MoveAlongPath(dot, circle), run_time=2, rate_func=linear)\n        self.play(Rotating(dot, about_point=[2, 0, 0]), run_time=1.5)\n        self.wait()"
    },
    {
      "id": "moving_around",
      "title": "MovingAround",
      "summary": "Applies simple transformations to a square.",
      "explanation": "Animates a square performing a sequence of basic transformations: shifting, changing color, scaling, and rotating.",
      "keywords": ["square", "shift", "color change", "scale", "rotate"],
      "example_text": "Demonstrate chained square transformations with the animate syntax.",
      "scene_family": "basic_transform_sequence",
      "symbols": ["Scene", "Square"],
      "tags": ["2d", "shape", "square", "animation", "transform"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass MovingAround(Scene):\n    def construct(self):\n        square = Square(color=BLUE, fill_opacity=1)\n        self.play(square.animate.shift(LEFT))\n        self.play(square.animate.set_fill(ORANGE))\n        self.play(square.animate.scale(0.3))\n        self.play(square.animate.rotate(0.4))"
    },
    {
      "id": "moving_angle",
      "title": "MovingAngle",
      "summary": "Uses a tracker and updaters to animate an angle and its label.",
      "explanation": "Uses a ValueTracker and updaters to create a dynamic angle animation where the label and arc follow the moving lines.",
      "keywords": ["angle", "updaters", "value tracker", "label", "dynamic geometry"],
      "example_text": "Animate an angle by updating a line, angle arc, and label from a shared ValueTracker.",
      "scene_family": "dynamic_angle",
      "symbols": ["Scene", "ValueTracker", "Line", "Angle", "MathTex"],
      "tags": ["2d", "math", "animation", "tracking", "geometry", "label"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass MovingAngle(Scene):\n    def construct(self):\n        rotation_center = LEFT\n        theta_tracker = ValueTracker(110)\n        line1 = Line(LEFT, RIGHT)\n        line_moving = Line(LEFT, RIGHT)\n        line_ref = line_moving.copy()\n        line_moving.rotate(theta_tracker.get_value() * DEGREES, about_point=rotation_center)\n        a = Angle(line1, line_moving, radius=0.5, other_angle=False)\n        tex = MathTex(r\"\\theta\").move_to(Angle(line1, line_moving, radius=0.5 + 3 * SMALL_BUFF, other_angle=False).point_from_proportion(0.5))\n        self.add(line1, line_moving, a, tex)\n        self.wait()\n        line_moving.add_updater(lambda x: x.become(line_ref.copy()).rotate(theta_tracker.get_value() * DEGREES, about_point=rotation_center))\n        a.add_updater(lambda x: x.become(Angle(line1, line_moving, radius=0.5, other_angle=False)))\n        tex.add_updater(lambda x: x.move_to(Angle(line1, line_moving, radius=0.5 + 3 * SMALL_BUFF, other_angle=False).point_from_proportion(0.5)))\n        self.play(theta_tracker.animate.set_value(40))\n        self.play(theta_tracker.animate.increment_value(140))\n        self.play(tex.animate.set_color(RED), run_time=0.5)\n        self.play(theta_tracker.animate.set_value(350))"
    },
    {
      "id": "moving_dots",
      "title": "MovingDots",
      "summary": "Updates two dots and a connecting line from trackers.",
      "explanation": "Features dots that move along different axes based on ValueTrackers, with a line connecting them that updates in real-time.",
      "keywords": ["dots", "line", "updaters", "value tracker", "real time"],
      "example_text": "Use trackers and updaters to move points independently while keeping a connecting line current.",
      "scene_family": "updater_connections",
      "symbols": ["Scene", "Dot", "VGroup", "Line", "ValueTracker"],
      "tags": ["2d", "point", "line", "tracking", "animation", "group"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass MovingDots(Scene):\n    def construct(self):\n        d1, d2 = Dot(color=BLUE), Dot(color=GREEN)\n        dg = VGroup(d1, d2).arrange(RIGHT, buff=1)\n        l1 = Line(d1.get_center(), d2.get_center()).set_color(RED)\n        x = ValueTracker(0)\n        y = ValueTracker(0)\n        d1.add_updater(lambda z: z.set_x(x.get_value()))\n        d2.add_updater(lambda z: z.set_y(y.get_value()))\n        l1.add_updater(lambda z: z.become(Line(d1.get_center(), d2.get_center())))\n        self.add(d1, d2, l1)\n        self.play(x.animate.set_value(5))\n        self.play(y.animate.set_value(4))\n        self.wait()"
    },
    {
      "id": "moving_group_to_destination",
      "title": "MovingGroupToDestination",
      "summary": "Moves a whole group of dots to a target point.",
      "explanation": "Animates a group of dots moving to a target destination, maintaining their relative spacing.",
      "keywords": ["group", "destination", "shift", "dots", "spacing"],
      "example_text": "Move a VGroup as one object toward a target destination dot.",
      "scene_family": "group_motion",
      "symbols": ["Scene", "VGroup", "Dot"],
      "tags": ["2d", "group", "point", "animation", "movement"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass MovingGroupToDestination(Scene):\n    def construct(self):\n        group = VGroup(Dot(LEFT), Dot(ORIGIN), Dot(RIGHT, color=RED), Dot(2 * RIGHT)).scale(1.4)\n        dest = Dot([4, 3, 0], color=YELLOW)\n        self.add(group, dest)\n        self.play(group.animate.shift(dest.get_center() - group[2].get_center()))\n        self.wait(0.5)"
    },
    {
      "id": "moving_frame_box",
      "title": "MovingFrameBox",
      "summary": "Moves a highlight box across parts of an equation.",
      "explanation": "Creates a LaTeX formula and uses a rectangular frame that jumps from one part of the formula to another using ReplacementTransform.",
      "keywords": ["equation", "frame box", "highlight", "replacement transform", "latex"],
      "example_text": "Highlight different pieces of a MathTex formula using SurroundingRectangle and ReplacementTransform.",
      "scene_family": "equation_highlight",
      "symbols": ["Scene", "MathTex", "Write", "SurroundingRectangle", "Create", "ReplacementTransform"],
      "tags": ["2d", "math", "equation", "highlight", "animation", "transform"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass MovingFrameBox(Scene):\n    def construct(self):\n        text = MathTex(\"\\\\frac{d}{dx} f(x)g(x)=\", \"f(x) \\\\frac{d}{dx} g(x)\", \"+\", \"g(x) \\\\frac{d}{dx} f(x)\")\n        self.play(Write(text))\n        framebox1 = SurroundingRectangle(text[1], buff=.1)\n        framebox2 = SurroundingRectangle(text[3], buff=.1)\n        self.play(Create(framebox1))\n        self.wait()\n        self.play(ReplacementTransform(framebox1, framebox2))\n        self.wait()"
    },
    {
      "id": "rotation_updater",
      "title": "RotationUpdater",
      "summary": "Uses custom updater functions to rotate a line back and forth.",
      "explanation": "Uses custom updater functions to rotate a yellow line around the origin, first clockwise and then counter-clockwise.",
      "keywords": ["rotation", "updater", "line", "clockwise", "counter clockwise"],
      "example_text": "Attach and swap custom updater functions to control line rotation over time.",
      "scene_family": "custom_updater_motion",
      "symbols": ["Scene", "Line"],
      "tags": ["2d", "animation", "line", "rotation", "updater"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass RotationUpdater(Scene):\n    def construct(self):\n        def updater_forth(mobj, dt):\n            mobj.rotate_about_origin(dt)\n        def updater_back(mobj, dt):\n            mobj.rotate_about_origin(-dt)\n        line_reference = Line(ORIGIN, LEFT).set_color(WHITE)\n        line_moving = Line(ORIGIN, LEFT).set_color(YELLOW)\n        line_moving.add_updater(updater_forth)\n        self.add(line_reference, line_moving)\n        self.wait(2)\n        line_moving.remove_updater(updater_forth)\n        line_moving.add_updater(updater_back)\n        self.wait(2)\n        line_moving.remove_updater(updater_back)\n        self.wait(0.5)"
    },
    {
      "id": "point_with_trace",
      "title": "PointWithTrace",
      "summary": "Draws a persistent trace behind a moving dot.",
      "explanation": "Shows a dot moving in a complex path with a persistent trace (path) drawn behind it using an updater.",
      "keywords": ["trace", "path", "dot", "updater", "vmobject"],
      "example_text": "Use a VMobject path with an updater to leave a visible trail behind a moving point.",
      "scene_family": "path_trace",
      "symbols": ["Scene", "VMobject", "Dot", "Rotating"],
      "tags": ["2d", "point", "path", "animation", "updater"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass PointWithTrace(Scene):\n    def construct(self):\n        path = VMobject()\n        dot = Dot()\n        path.set_points_as_corners([dot.get_center(), dot.get_center()])\n        def update_path(path):\n            previous_path = path.copy()\n            previous_path.add_points_as_corners([dot.get_center()])\n            path.become(previous_path)\n        path.add_updater(update_path)\n        self.add(path, dot)\n        self.play(Rotating(dot, angle=PI, about_point=RIGHT, run_time=2))\n        self.wait()\n        self.play(dot.animate.shift(UP))\n        self.play(dot.animate.shift(LEFT))\n        self.wait()"
    },
    {
      "id": "sin_and_cos_function_plot",
      "title": "SinAndCosFunctionPlot",
      "summary": "Plots sine and cosine with labels and a vertical marker.",
      "explanation": "Plots sine and cosine functions on a set of axes with labels and a specific vertical line at a TAU coordinate.",
      "keywords": ["sine", "cosine", "axes", "plot", "graph labels"],
      "example_text": "Create axes, plot trigonometric functions, and add labels and guide lines.",
      "scene_family": "function_plot",
      "symbols": ["Scene", "Axes", "VGroup", "Line"],
      "tags": ["2d", "graphing", "axes", "plot", "math", "label"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass SinAndCosFunctionPlot(Scene):\n    def construct(self):\n        axes = Axes(x_range=[-10, 10.3, 1], y_range=[-1.5, 1.5, 1], x_length=10, axis_config={\"color\": GREEN}, x_axis_config={\"numbers_to_include\": np.arange(-10, 10.01, 2), \"numbers_with_elongated_ticks\": np.arange(-10, 10.01, 2)}, tips=False)\n        axes_labels = axes.get_axis_labels()\n        sin_graph = axes.plot(lambda x: np.sin(x), color=BLUE)\n        cos_graph = axes.plot(lambda x: np.cos(x), color=RED)\n        sin_label = axes.get_graph_label(sin_graph, \"\\\\sin(x)\", x_val=-10, direction=UP / 2)\n        cos_label = axes.get_graph_label(cos_graph, label=\"\\\\cos(x)\")\n        vert_line = axes.get_vertical_line(axes.i2gp(TAU, cos_graph), color=YELLOW, line_func=Line)\n        line_label = axes.get_graph_label(cos_graph, r\"x=2\\pi\", x_val=TAU, direction=UR, color=WHITE)\n        plot = VGroup(axes, sin_graph, cos_graph, vert_line)\n        labels = VGroup(axes_labels, sin_label, cos_label, line_label)\n        self.add(plot, labels)"
    },
    {
      "id": "argmin_example",
      "title": "ArgMinExample",
      "summary": "Moves a dot to the minimum of a plotted quadratic.",
      "explanation": "Finds and animates a dot moving to the minimum point of a quadratic function using NumPy's argmin.",
      "keywords": ["minimum", "argmin", "quadratic", "dot", "tracker"],
      "example_text": "Plot a function and animate a point moving to its minimum value using NumPy and ValueTracker.",
      "scene_family": "optimization_plot",
      "symbols": ["Scene", "Axes", "ValueTracker", "Dot"],
      "tags": ["2d", "graphing", "plot", "math", "point", "tracking"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass ArgMinExample(Scene):\n    def construct(self):\n        ax = Axes(x_range=[0, 10], y_range=[0, 100, 10], axis_config={\"include_tip\": False})\n        labels = ax.get_axis_labels(x_label=\"x\", y_label=\"f(x)\")\n        t = ValueTracker(0)\n        def func(x):\n            return 2 * (x - 5) ** 2\n        graph = ax.plot(func, color=MAROON)\n        initial_point = [ax.coords_to_point(t.get_value(), func(t.get_value()))]\n        dot = Dot(point=initial_point)\n        dot.add_updater(lambda x: x.move_to(ax.c2p(t.get_value(), func(t.get_value()))))\n        x_space = np.linspace(*ax.x_range[:2], 200)\n        minimum_index = func(x_space).argmin()\n        self.add(ax, labels, graph, dot)\n        self.play(t.animate.set_value(x_space[minimum_index]))\n        self.wait()"
    },
    {
      "id": "graph_area_plot",
      "title": "GraphAreaPlot",
      "summary": "Shows curves, Riemann rectangles, and shaded area between graphs.",
      "explanation": "Demonstrates plotting multiple curves and shading the area between them, along with showing Riemann rectangles.",
      "keywords": ["area", "riemann rectangles", "curves", "axes", "shading"],
      "example_text": "Use Axes to plot multiple curves and visualize areas and Riemann rectangles.",
      "scene_family": "area_visualization",
      "symbols": ["Scene", "Axes"],
      "tags": ["2d", "graphing", "plot", "area", "math"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass GraphAreaPlot(Scene):\n    def construct(self):\n        ax = Axes(x_range=[0, 5], y_range=[0, 6], x_axis_config={\"numbers_to_include\": [2, 3]}, tips=False)\n        labels = ax.get_axis_labels()\n        curve_1 = ax.plot(lambda x: 4 * x - x ** 2, x_range=[0, 4], color=BLUE_C)\n        curve_2 = ax.plot(lambda x: 0.8 * x ** 2 - 3 * x + 4, x_range=[0, 4], color=GREEN_B)\n        line_1 = ax.get_vertical_line(ax.input_to_graph_point(2, curve_1), color=YELLOW)\n        line_2 = ax.get_vertical_line(ax.i2gp(3, curve_1), color=YELLOW)\n        riemann_area = ax.get_riemann_rectangles(curve_1, x_range=[0.3, 0.6], dx=0.03, color=BLUE, fill_opacity=0.5)\n        area = ax.get_area(curve_2, [2, 3], bounded_graph=curve_1, color=GREY, opacity=0.5)\n        self.add(ax, labels, curve_1, curve_2, line_1, line_2, riemann_area, area)"
    },
    {
      "id": "polygon_on_axes",
      "title": "PolygonOnAxes",
      "summary": "Updates a rectangle based on a moving point on a graph.",
      "explanation": "Animates a rectangle (polygon) whose dimensions update based on a point moving along a reciprocal function graph.",
      "keywords": ["polygon", "axes", "always_redraw", "tracker", "reciprocal"],
      "example_text": "Use always_redraw and a ValueTracker to keep a polygon synchronized with a moving graph point.",
      "scene_family": "dynamic_area_shape",
      "symbols": ["Scene", "Axes", "ValueTracker", "Polygon", "Dot", "Create"],
      "tags": ["2d", "graphing", "polygon", "tracking", "animation", "math"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass PolygonOnAxes(Scene):\n    def get_rectangle_corners(self, bottom_left, top_right):\n        return [(top_right[0], top_right[1]), (bottom_left[0], top_right[1]), (bottom_left[0], bottom_left[1]), (top_right[0], bottom_left[1])]\n    def construct(self):\n        ax = Axes(x_range=[0, 10], y_range=[0, 10], x_length=6, y_length=6, axis_config={\"include_tip\": False})\n        t = ValueTracker(5)\n        k = 25\n        graph = ax.plot(lambda x: k / x, color=YELLOW_D, x_range=[k / 10, 10.0, 0.01], use_smoothing=False)\n        def get_rectangle():\n            polygon = Polygon(*[ax.c2p(*i) for i in self.get_rectangle_corners((0, 0), (t.get_value(), k / t.get_value()))])\n            polygon.stroke_width = 1\n            polygon.set_fill(BLUE, opacity=0.5)\n            polygon.set_stroke(YELLOW_B)\n            return polygon\n        polygon = always_redraw(get_rectangle)\n        dot = Dot()\n        dot.add_updater(lambda x: x.move_to(ax.c2p(t.get_value(), k / t.get_value())))\n        dot.set_z_index(10)\n        self.add(ax, graph, dot)\n        self.play(Create(polygon))\n        self.play(t.animate.set_value(10))\n        self.play(t.animate.set_value(k / 10))\n        self.play(t.animate.set_value(5))"
    },
    {
      "id": "heat_diagram_plot",
      "title": "HeatDiagramPlot",
      "summary": "Draws a custom line graph from explicit coordinate values.",
      "explanation": "Plots a custom line graph representing temperature change against heat using defined coordinate points.",
      "keywords": ["line graph", "axes", "coordinates", "temperature", "data"],
      "example_text": "Plot a custom line graph from x and y data arrays on labeled axes.",
      "scene_family": "data_line_plot",
      "symbols": ["Scene", "Axes", "Tex"],
      "tags": ["2d", "graphing", "plot", "data", "axes", "label"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass HeatDiagramPlot(Scene):\n    def construct(self):\n        ax = Axes(x_range=[0, 40, 5], y_range=[-8, 32, 5], x_length=9, y_length=6, x_axis_config={\"numbers_to_include\": np.arange(0, 40, 5)}, y_axis_config={\"numbers_to_include\": np.arange(-5, 34, 5)}, tips=False)\n        labels = ax.get_axis_labels(x_label=Tex(r\"$\\Delta Q$\"), y_label=Tex(r\"T[$^\\circ C$]\"))\n        x_vals = [0, 8, 38, 39]\n        y_vals = [20, 0, 0, -5]\n        graph = ax.plot_line_graph(x_values=x_vals, y_values=y_vals)\n        self.add(ax, labels, graph)"
    },
    {
      "id": "following_graph_camera",
      "title": "FollowingGraphCamera",
      "summary": "Makes the camera frame follow a dot on a graph.",
      "explanation": "Uses a MovingCameraScene to make the camera frame follow a dot as it moves along a sine curve.",
      "keywords": ["camera", "moving camera", "dot", "graph", "follow"],
      "example_text": "Use MovingCameraScene and a frame updater to follow a moving point along a graph.",
      "scene_family": "camera_follow",
      "symbols": ["MovingCameraScene", "Axes", "Dot", "MoveAlongPath", "Restore"],
      "tags": ["2d", "camera", "graphing", "plot", "point", "movement"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass FollowingGraphCamera(MovingCameraScene):\n    def construct(self):\n        self.camera.frame.save_state()\n        ax = Axes(x_range=[-1, 10], y_range=[-1, 10])\n        graph = ax.plot(lambda x: np.sin(x), color=BLUE, x_range=[0, 3 * PI])\n        moving_dot = Dot(ax.i2gp(graph.t_min, graph), color=ORANGE)\n        dot_1 = Dot(ax.i2gp(graph.t_min, graph))\n        dot_2 = Dot(ax.i2gp(graph.t_max, graph))\n        self.add(ax, graph, dot_1, dot_2, moving_dot)\n        self.play(self.camera.frame.animate.scale(0.5).move_to(moving_dot))\n        def update_curve(mob):\n            mob.move_to(moving_dot.get_center())\n        self.camera.frame.add_updater(update_curve)\n        self.play(MoveAlongPath(moving_dot, graph, rate_func=linear))\n        self.camera.frame.remove_updater(update_curve)\n        self.play(Restore(self.camera.frame))"
    },
    {
      "id": "moving_zoomed_scene_around",
      "title": "MovingZoomedSceneAround",
      "summary": "Demonstrates a zoomed scene with a pop-out display.",
      "explanation": "An advanced camera example demonstrating the ZoomedScene feature to magnify specific areas of the animation.",
      "keywords": ["zoomed scene", "camera", "magnify", "display", "frame"],
      "example_text": "Use ZoomedScene with an image and a tracked frame to show a magnified inset display.",
      "scene_family": "zoomed_camera",
      "symbols": ["ZoomedScene", "Dot", "ImageMobject", "Text", "BackgroundRectangle", "Create", "FadeIn", "Uncreate"],
      "tags": ["2d", "camera", "image", "zoom", "text", "animation"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass MovingZoomedSceneAround(ZoomedScene):\n    def __init__(self, **kwargs):\n        ZoomedScene.__init__(self, zoom_factor=0.3, zoomed_display_height=1, zoomed_display_width=6, image_frame_stroke_width=20, zoomed_camera_config={\"default_frame_stroke_width\": 3}, **kwargs)\n    def construct(self):\n        dot = Dot().shift(UL * 2)\n        image = ImageMobject(np.uint8([[0, 100, 30, 200], [255, 0, 5, 33]]))\n        image.height = 7\n        frame_text = Text(\"Frame\", color=PURPLE, font_size=67)\n        zoomed_camera_text = Text(\"Zoomed camera\", color=RED, font_size=67)\n        self.add(image, dot)\n        zoomed_camera = self.zoomed_camera\n        zoomed_display = self.zoomed_display\n        frame = zoomed_camera.frame\n        zoomed_display_frame = zoomed_display.display_frame\n        frame.move_to(dot)\n        frame.set_color(PURPLE)\n        zoomed_display_frame.set_color(RED)\n        zoomed_display.shift(DOWN)\n        zd_rect = BackgroundRectangle(zoomed_display, fill_opacity=0, buff=MED_SMALL_BUFF)\n        self.add_foreground_mobject(zd_rect)\n        unfold_camera = UpdateFromFunc(zd_rect, lambda rect: rect.replace(zoomed_display))\n        frame_text.next_to(frame, DOWN)\n        self.play(Create(frame), FadeIn(frame_text, shift=UP))\n        self.activate_zooming()\n        self.play(self.get_zoomed_display_pop_out_animation(), unfold_camera)\n        zoomed_camera_text.next_to(zoomed_display_frame, DOWN)\n        self.play(FadeIn(zoomed_camera_text, shift=UP))\n        scale_factor = [0.5, 1.5, 0]\n        self.play(frame.animate.scale(scale_factor), zoomed_display.animate.scale(scale_factor), FadeOut(zoomed_camera_text), FadeOut(frame_text))\n        self.wait()\n        self.play(ScaleInPlace(zoomed_display, 2))\n        self.wait()\n        self.play(frame.animate.shift(2.5 * DOWN))\n        self.wait()\n        self.play(self.get_zoomed_display_pop_out_animation(), unfold_camera, rate_func=lambda t: smooth(1 - t))\n        self.play(Uncreate(zoomed_display_frame), FadeOut(frame))\n        self.wait()"
    },
    {
      "id": "fixed_in_frame_mobject_test",
      "title": "FixedInFrameMObjectTest",
      "summary": "Keeps text fixed in frame while showing a 3D scene.",
      "explanation": "Shows how to lock a 2D text element in place (fixed in frame) while navigating a 3D scene with axes.",
      "keywords": ["3d", "fixed in frame", "text", "axes", "camera"],
      "example_text": "Use add_fixed_in_frame_mobjects to keep a text label fixed during a 3D scene.",
      "scene_family": "fixed_overlay_in_3d",
      "symbols": ["ThreeDScene", "ThreeDAxes", "Text"],
      "tags": ["3d", "camera", "text", "axes", "overlay"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass FixedInFrameMObjectTest(ThreeDScene):\n    def construct(self):\n        axes = ThreeDAxes()\n        self.set_camera_orientation(phi=75 * DEGREES, theta=-45 * DEGREES)\n        text3d = Text(\"This is a 3D text\")\n        self.add_fixed_in_frame_mobjects(text3d)\n        text3d.to_corner(UL)\n        self.add(axes)\n        self.wait()"
    },
    {
      "id": "three_d_light_source_position",
      "title": "ThreeDLightSourcePosition",
      "summary": "Changes the 3D light source position around a surface.",
      "explanation": "Illustrates a 3D surface (sphere) and demonstrates how to manually change the position of the camera's light source.",
      "keywords": ["3d", "surface", "light source", "sphere", "camera"],
      "example_text": "Move the renderer camera light source in a ThreeDScene with a parametric surface.",
      "scene_family": "3d_lighting",
      "symbols": ["ThreeDScene", "ThreeDAxes", "Surface"],
      "tags": ["3d", "surface", "camera", "light", "axes"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass ThreeDLightSourcePosition(ThreeDScene):\n    def construct(self):\n        axes = ThreeDAxes()\n        sphere = Surface(lambda u, v: np.array([1.5 * np.cos(u) * np.cos(v), 1.5 * np.cos(u) * np.sin(v), 1.5 * np.sin(u)]), v_range=[0, TAU], u_range=[-PI / 2, PI / 2], checkerboard_colors=[RED_D, RED_E], resolution=(15, 32))\n        self.renderer.camera.light_source.move_to(3 * IN)\n        self.set_camera_orientation(phi=75 * DEGREES, theta=30 * DEGREES)\n        self.add(axes, sphere)"
    },
    {
      "id": "three_d_camera_rotation",
      "title": "ThreeDCameraRotation",
      "summary": "Rotates the 3D camera around a simple scene.",
      "explanation": "Shows a 3D scene where the camera rotates around the origin continuously using ambient rotation methods.",
      "keywords": ["3d", "camera rotation", "ambient rotation", "circle", "axes"],
      "example_text": "Use begin_ambient_camera_rotation and stop_ambient_camera_rotation in a ThreeDScene.",
      "scene_family": "3d_camera_motion",
      "symbols": ["ThreeDScene", "ThreeDAxes", "Circle"],
      "tags": ["3d", "camera", "rotation", "axes", "shape"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass ThreeDCameraRotation(ThreeDScene):\n    def construct(self):\n        axes = ThreeDAxes()\n        circle = Circle()\n        self.set_camera_orientation(phi=75 * DEGREES, theta=30 * DEGREES)\n        self.add(circle, axes)\n        self.begin_ambient_camera_rotation(rate=0.1)\n        self.wait()\n        self.stop_ambient_camera_rotation()\n        self.move_camera(phi=75 * DEGREES, theta=30 * DEGREES)\n        self.wait()"
    },
    {
      "id": "three_d_camera_illusion_rotation",
      "title": "ThreeDCameraIllusionRotation",
      "summary": "Uses Manim's 3D illusion camera rotation effect.",
      "explanation": "Applies a '3D illusion' camera rotation effect which oscillates the camera angle to create a sense of depth.",
      "keywords": ["3d", "camera illusion", "rotation", "depth", "axes"],
      "example_text": "Use begin_3dillusion_camera_rotation to create a depth illusion around a simple 3D scene.",
      "scene_family": "3d_camera_motion",
      "symbols": ["ThreeDScene", "ThreeDAxes", "Circle"],
      "tags": ["3d", "camera", "rotation", "illusion", "axes"],
      "source": "manim_user_dataset",
      "code": "from manim import *\n\nclass ThreeDCameraIllusionRotation(ThreeDScene):\n    def construct(self):\n        axes = ThreeDAxes()\n        circle = Circle()\n        self.set_camera_orientation(phi=75 * DEGREES, theta=30 * DEGREES)\n        self.add(circle, axes)\n        self.begin_3dillusion_camera_rotation(rate=2)\n        self.wait(PI / 2)\n        self.stop_3dillusion_camera_rotation()"
    },
    {
      "id": "three_d_surface_plot",
      "title": "ThreeDSurfacePlot",
      "summary": "Plots a checkerboard-colored Gaussian surface in 3D.",
      "explanation": "Plots a 3D Gaussian surface with a checkerboard texture on a 3D coordinate system.",
      "keywords": ["3d", "surface", "gaussian", "checkerboard", "axes"],
      "example_text": "Create a parametric surface, style it, and display it on ThreeDAxes in a ThreeDScene.",
      "scene_family": "3d_surface_plot",
      "symbols": ["ThreeDScene", "Surface", "ThreeDAxes"],
      "tags": ["3d", "surface", "graphing", "axes", "math"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass ThreeDSurfacePlot(ThreeDScene):\n    def construct(self):\n        resolution_fa = 24\n        self.set_camera_orientation(phi=75 * DEGREES, theta=-30 * DEGREES)\n        def param_gauss(u, v):\n            x, y = u, v\n            sigma, mu = 0.4, [0.0, 0.0]\n            d = np.linalg.norm(np.array([x - mu[0], y - mu[1]]))\n            z = np.exp(-(d**2 / (2.0 * sigma**2)))\n            return np.array([x, y, z])\n        gauss_plane = Surface(param_gauss, resolution=(resolution_fa, resolution_fa), v_range=[-2, +2], u_range=[-2, +2])\n        gauss_plane.scale(2, about_point=ORIGIN)\n        gauss_plane.set_style(fill_opacity=1, stroke_color=GREEN)\n        gauss_plane.set_fill_by_checkerboard(ORANGE, BLUE, opacity=0.5)\n        axes = ThreeDAxes()\n        self.add(axes, gauss_plane)"
    },
    {
      "id": "opening_manim",
      "title": "OpeningManim",
      "summary": "Combines text, formula transforms, grid creation, and nonlinear deformation.",
      "explanation": "A comprehensive example showing text, LaTeX summation, coordinate grids, and non-linear coordinate transformations.",
      "keywords": ["latex", "transform", "grid", "nonlinear transform", "opening scene"],
      "example_text": "A multi-step scene that writes text, transforms it, creates a grid, and applies a nonlinear function.",
      "scene_family": "multi_stage_intro",
      "symbols": ["Scene", "Tex", "MathTex", "VGroup", "Write", "FadeIn", "Transform", "NumberPlane", "Create"],
      "tags": ["2d", "text", "math", "graphing", "transform", "animation"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass OpeningManim(Scene):\n    def construct(self):\n        title = Tex(r\"This is some \\LaTeX\")\n        basel = MathTex(r\"\\sum_{n=1}^\\infty \\frac{1}{n^2} = \\frac{\\pi^2}{6}\")\n        VGroup(title, basel).arrange(DOWN)\n        self.play(Write(title), FadeIn(basel, shift=DOWN))\n        self.wait()\n        transform_title = Tex(\"That was a transform\")\n        transform_title.to_corner(UP + LEFT)\n        self.play(Transform(title, transform_title), LaggedStart(*[FadeOut(obj, shift=DOWN) for obj in basel]))\n        self.wait()\n        grid = NumberPlane()\n        grid_title = Tex(\"This is a grid\", font_size=72)\n        grid_title.move_to(transform_title)\n        self.add(grid, grid_title)\n        self.play(FadeOut(title), FadeIn(grid_title, shift=UP), Create(grid, run_time=3, lag_ratio=0.1))\n        self.wait()\n        grid_transform_title = Tex(r\"That was a non-linear function \\\\ applied to the grid\")\n        grid_transform_title.move_to(grid_title, UL)\n        grid.prepare_for_nonlinear_transform()\n        self.play(grid.animate.apply_function(lambda p: p + np.array([np.sin(p[1]), np.sin(p[0]), 0])), run_time=3)\n        self.wait()\n        self.play(Transform(grid_title, grid_transform_title))\n        self.wait()"
    },
    {
      "id": "sine_curve_unit_circle",
      "title": "SineCurveUnitCircle",
      "summary": "Draws a sine wave from a dot rotating around a unit circle.",
      "explanation": "Animates the relationship between a unit circle and a sine wave, drawing the curve as a dot orbits the circle.",
      "keywords": ["unit circle", "sine curve", "dot", "orbit", "trace"],
      "example_text": "Relate circular motion to sine graph generation using updaters and always_redraw helpers.",
      "scene_family": "unit_circle_trig",
      "symbols": ["Scene", "Line", "MathTex", "Circle", "Dot", "VGroup"],
      "tags": ["2d", "math", "graphing", "circle", "point", "animation"],
      "source": "manim_user_dataset",
      "code": "from manim import *\nimport numpy as np\n\nclass SineCurveUnitCircle(Scene):\n    def construct(self):\n        self.show_axis()\n        self.show_circle()\n        self.move_dot_and_draw_curve()\n        self.wait()\n    def show_axis(self):\n        x_axis = Line([-6, 0, 0], [6, 0, 0])\n        y_axis = Line([-4, -2, 0], [-4, 2, 0])\n        self.add(x_axis, y_axis)\n        self.add_x_labels()\n        self.origin_point = np.array([-4, 0, 0])\n        self.curve_start = np.array([-3, 0, 0])\n    def add_x_labels(self):\n        x_labels = [MathTex(r\"\\pi\"), MathTex(r\"2 \\pi\"), MathTex(r\"3 \\pi\"), MathTex(r\"4 \\pi\")]\n        for i in range(len(x_labels)):\n            x_labels[i].next_to(np.array([-1 + 2 * i, 0, 0]), DOWN)\n            self.add(x_labels[i])\n    def show_circle(self):\n        circle = Circle(radius=1)\n        circle.move_to(self.origin_point)\n        self.add(circle)\n        self.circle = circle\n    def move_dot_and_draw_curve(self):\n        orbit = self.circle\n        origin_point = self.origin_point\n        dot = Dot(radius=0.08, color=YELLOW)\n        dot.move_to(orbit.point_from_proportion(0))\n        self.t_offset = 0\n        rate = 0.25\n        def go_around_circle(mob, dt):\n            self.t_offset += (dt * rate)\n            mob.move_to(orbit.point_from_proportion(self.t_offset % 1))\n        def get_line_to_circle():\n            return Line(origin_point, dot.get_center(), color=BLUE)\n        def get_line_to_curve():\n            x = self.curve_start[0] + self.t_offset * 4\n            y = dot.get_center()[1]\n            return Line(dot.get_center(), np.array([x, y, 0]), color=YELLOW_A, stroke_width=2)\n        self.curve = VGroup()\n        self.curve.add(Line(self.curve_start, self.curve_start))\n        def get_curve():\n            last_line = self.curve[-1]\n            x = self.curve_start[0] + self.t_offset * 4\n            y = dot.get_center()[1]\n            new_line = Line(last_line.get_end(), np.array([x, y, 0]), color=YELLOW_D)\n            self.curve.add(new_line)\n            return self.curve\n        dot.add_updater(go_around_circle)\n        self.add(dot, orbit, always_redraw(get_line_to_circle), always_redraw(get_line_to_curve), always_redraw(get_curve))\n        self.wait(8.5)\n        dot.remove_updater(go_around_circle)"
    }
  ]
}
